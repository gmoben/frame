// import chai, {expect} from 'chai';
// chai.should();

// import App from 'server/core/App';
// import {models, config} from './fixtures';

'use strict';

describe('MongooseModel', function () {
  // let app = new App(models, config);

  beforeEach(function (done) {
    return done();
  });

  describe('#factory()', function () {
    it('creates a model from 3 arguments');
    it('takes an object as a single parameter and returns transformed arguments');
  });

  describe('#addSocket()', function () {
    it('adds a socketio instance as a listener');
  });
});
//# sourceMappingURL=../tests/MongooseModel.spec.js.map